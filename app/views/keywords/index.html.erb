<h1>Keyword#index</h1>
<p>Find me in app/views/keyword/index.html.erb</p>

<p style="color: green"><%= notice %></p>

<% if @keywords.empty? %>
<p><i>No keywords to display.</i></p>
<% else %>
<table class="table">

<thead>
<tr>
  <th scope="col">ID</th>
  <th scope="col">Name</th>
  <th scope="col">Category</th>
  <th scope="col">Edit</th>
  <th scope="col">Delete</th>
</tr>
</thead>

<% @keywords.each do |keyword| %>
<tr>
  <th scope="row"><%= keyword.id %></th>
  <td><%= keyword.name %></td>
  <td><%= keyword.category %></td>
  <td><%= link_to "Edit", edit_keyword_path(keyword) %></td>
  <td><%= button_to "Delete", keyword, method: :delete %></td>
</tr>
<% end %>

</table>
<% end %>
<%= link_to "Create new", new_keyword_path %>
