<%= form_with(model: social_media_site) do |form| %>
  <% if social_media_site.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(social_media_site.errors.count, "error") %> prohibited this social_media_site from being saved:</h2>

      <ul>
        <% social_media_site.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :logo, style: "display: block" %>
    <%= form.file_field :logo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
